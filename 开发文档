# MSP Music 完整手册


>
> 本手册是全部文档的**合并完整版**：使用教程 + 开发指南合为一篇，既适合普通用户查阅，也适合开发者按「想做什么效果 → 改哪个文件」的方式直接动手。


---

## 总目录

**第一篇：使用篇**

1. [这是什么软件](#一这是什么软件)
2. [快速开始（电脑版 + 安卓版）](#二快速开始电脑版--安卓版)
3. [功能详解](#三功能详解)
4. [在线音源：网易云 / B站](#四在线音源网易云--b站)
5. [局域网互通](#五局域网互通)
6. [常见卡点 FAQ](#六常见卡点-faq)

**第二篇：开发篇**

7. [架构心智模型](#七架构心智模型)
8. [文件职责地图](#八文件职责地图)
9. [数据流详解](#九数据流详解)
10. [「想改什么效果 → 动哪里」对照表](#十想改什么效果--动哪里对照表)
11. [局域网协议详解](#十一局域网协议详解)
12. [DSP 音效引擎详解](#十二dsp-音效引擎详解)
13. [开发环境、构建与打包](#十三开发环境构建与打包)
14. [调试方法大全](#十四调试方法大全)
15. [持久化数据清单](#十五持久化数据清单)
16. [设计与动画规范](#十六设计与动画规范)
17. [历史修复（别回退）与踩坑清单](#十七历史修复别回退与踩坑清单)
18. [验证清单与上手路线](#十八验证清单与上手路线)

**附录**

- [附录 A：支持的音频格式](#附录-a支持的音频格式)
- [附录 B：快捷键一览](#附录-b快捷键一览)
- [附录 C：术语表](#附录-c术语表)
- [免责声明](#免责声明)
- [开源声明](#开源声明)

---

# 第一篇：使用篇

## 一、这是什么软件

MSP Music 是一款以「**本地音乐**」为核心的播放器，有两个端：

| 端 | 技术 | 形态 |
|---|---|---|
| 电脑版 | Electron（Windows / Linux） | 桌面应用，侧边栏 + 主内容区 + 底部播放栏 |
| 安卓版 | Java 外壳 + Media3 + 单个 WebView | APK，界面同样是 HTML，息屏可播、通知栏可控 |

两端在同一 WiFi（局域网）下可以**互相访问对方的曲库**，用 6 位配对码授权，无需拷贝文件、无需注册账号。

核心能力一览：

- 🎵 本地音乐播放：自动扫描、读标签（歌名/歌手/专辑/封面）、`.cue` 整轨自动拆曲
- 📃 歌词：本地同名 `.lrc` 优先，缺失时在线抓取，逐行高亮滚动
- 🎚 调音台：均衡器、混响、延迟校正、动态控制、Crossfeed、声场工具（DSP 有单元测试守护）
- 🌐 在线音源：网易云音乐、哔哩哔哩（电脑版还支持 QQ 音乐、B站 MV 模式）
- 📱 横屏 / 竖屏自适应：横屏为「左舞台 + 右控制」布局，封面始终正方形
- 🖼 自定义背景（图片 / 视频）、深色 / 浅色主题
- 🔁 随机 / 顺序 / 单曲循环、倍速、AB 复读、断点记忆

---

## 二、快速开始（电脑版 + 安卓版）

先让一首歌响起来，三步就够。

### 2.1 电脑版（Windows）

**第 1 步：启动**

- 已安装：开始菜单打开 **MSP Music**
- 绿色版：双击 `MSPMusic.exe`
- 开发模式：项目根目录执行 `npx electron .`

**第 2 步：添加音乐文件夹**

首次启动出现空页面引导，点 **「添加音乐文件夹」**；之后可在 **设置** 里继续添加。

- 支持多个文件夹，曲库是它们的合集
- 扫描后自动读取文件标签与内嵌封面；`.cue` 自动按曲目拆开
- 支持格式见 [附录 A](#附录-a支持的音频格式)

**第 3 步：播放**

点主页任意歌曲即开始播放，底部出现播放栏。

- 空格键：播放 / 暂停；其他快捷键见 [附录 B](#附录-b快捷键一览)
- 点底部播放栏：进入全屏歌词页
- 随机 / 循环按钮在底部栏；音量、进度直接拖动

### 2.2 安卓版

**第 1 步：安装 APK**

当前安装包：[apk/MSPMusic-a-m10-m2m3.apk](../apk/MSPMusic-a-m10-m2m3.apk)，传到手机点击安装，允许「安装未知来源应用」。

> ⚠️ **冷启动白屏 7～12 秒是正常的**，WebView 在初始化，不是死机。

**第 2 步：添加音乐文件夹（SAF 授权，不需要存储权限）**

1. 打开 App → 点 **「添加音乐文件夹」**
2. 系统文件选择器中选中音乐文件夹，点 **「使用此文件夹 / 允许」**
3. 返回后自动扫描，歌曲出现在主页

**第 3 步：播放**

- 点歌曲即播，由系统原生 Media3 出声：息屏继续、通知栏控制、蓝牙耳机按键、车机 AVRCP 都支持
- 底部 mini 栏点一下进入全屏播放页
- 底部六个 Tab：主页 / 搜索 / 局域网 / 队列 / 收藏 / 设置

### 2.3 第一次使用的建议路径

1. 先加一个只有几首歌的文件夹，确认能出声
2. 放一首带内嵌封面的 MP3，确认封面
3. 放一首同目录有同名 `.lrc` 的歌，确认歌词滚动
4. 再去研究主题、调音台、在线音源、局域网互通

---

## 三、功能详解

### 3.1 播放控制

**电脑版底部播放栏**：封面、歌名歌手、上一首 / 播放暂停 / 下一首、进度条、音量、随机与循环、播放引擎状态；点封面/歌名进入全屏歌词页，底部还有「信号路径」面板可查看当前音频链路。

**安卓版**：

- mini 栏常驻底部（毛玻璃）：封面、标题、收藏、播放暂停、下一首、进度细线
- 全屏播放页：大封面与歌词可互相切换；底部控制区有循环模式、上一首、播放暂停、下一首、随机；进度条拖动
- 媒体通知与锁屏：系统 MediaSession 自动生成，支持蓝牙/线控耳机按键

**播放模式**：顺序循环（默认，播完整队列从头再来）、单曲循环、随机播放。随机由原生播放器（Media3）侧打乱队列实现。

**倍速播放**：可选 0.5×～2×，变调不变速感（系统播放器处理）。

### 3.2 曲库与文件夹

- 支持同时添加多个文件夹；曲库为合集
- 刷新音乐库：重新扫描所有文件夹，增量合入新歌、剪除已删除文件，封面/元数据缓存复用不会全部重来
- 曲库条目包含：标题、歌手、专辑、时长、封面来源标记
- 安卓端文件夹授权基于 SAF（Storage Access Framework），选定即 `takePersistableUriPermission` 长期有效，App **不申请存储权限**

### 3.3 封面

封面的获取优先级：

1. **音乐文件内嵌封面**（ID3 APIC / FLAC METADATA_BLOCK_PICTURE 等）
2. 无内嵌封面时，**在线刮取**：QQ → 网易云 → LRCLIB（按标题+歌手匹配打分）
3. 都没有：音符占位图标

- 全屏页封面锁**正方形**，不会随屏幕拉伸
- 在线封面直链若被源站按 Referer 拦截（裂图），App 自动改走内置图片代理加载
- 刮取过程中封面增量刷新，不重建列表、不闪屏；进度看右上角悬浮圆圈

### 3.4 歌词

歌词来源与顺序：

1. 歌曲同目录下的**同名 `.lrc`**（如 `告白气球.mp3` + `告白气球.lrc`）
2. 没有本地歌词 → 在线抓取（与封面刮取同一套匹配流程）

歌词页能力：

- 当前播放行高亮、自动滚动；手动滚动后暂停自动跟随，停止操作约 2.5 秒后回到播放行
- 全屏页可在「封面 / 歌词」间切换
- 时间轴整体偏移时可用歌词偏移校正
- 在线歌曲（网易云等）的歌词也会自动接入
- 简繁转换：歌词与标签可转简体（电脑端用 OpenCC）

### 3.5 主题与自定义背景

- 三种主题：跟随系统 / 浅色 / 深色；切换时系统状态栏深浅联动
- 自定义背景：图片或视频，固定在所有内容之下，主题底色始终兜底（不会出现透明穿帮）
- 开启自定义背景后，侧栏近乎全透明，只突出内容

### 3.6 播放增强功能

- **AB 复读**：设定 A 点、B 点，播放到 B 自动跳回 A，适合练琴/学语言
- **断点记忆**：长视频/歌曲退出后再播，可从上次位置继续
- **Gapless 预载**：队列相邻曲目无缝衔接（原生播放器能力）
- **CUE 分轨**：整轨 `.cue` 列表自动拆成独立曲目
- **高级随机**：避免随机模式下短期内重复

### 3.7 MV 模式（电脑版）

- 一键搜索当前歌曲的 B站 MV 并播放
- 1080P DASH 音视频流合并播放
- MV 进度自动保存；退出全屏再返回，画面与进度不丢
- 可在视频画面上叠加歌词
- 系统级全屏切换

### 3.8 账号与登录（电脑版）

- 网易云 / QQ 音乐支持账号功能：扫码登录、网页登录、短信登录
- 登录在平台官方页面完成，App 不接触账号密码
- Cookie 只保存在本机，仅用于维持登录态，不上传第三方

---

## 四、在线音源：网易云 / B站

本地曲库之外，可直接搜索播放在线歌曲。音频流来自平台官方 CDN，仅供个人试听；VIP / 无版权内容无法绕过。

### 4.1 切换音源（安卓）

搜索页搜索框下方三个 chip：

| Chip | 颜色 | 来源 |
|---|---|---|
| 本地 | 蓝色（主题色） | 手机本地曲库 |
| 网易云 | 红色 `#e03a3e` | 网易云音乐 |
| B站 | 粉色 `#fb7299` | 哔哩哔哩（取视频音轨） |

选择自动记住（localStorage `msp.search.src`）。电脑版在顶部搜索，结果按来源分组筛选（本地 / 网易云 / QQ音乐 / B站）。

### 4.2 网易云音乐

- 搜索结果含歌名、歌手、专辑、时长、封面
- 点歌时**批量解析整页结果**的播放地址（320kbps），解析完才开始；之后整页连播中途不再等待
- VIP / 无版权歌曲播放地址为空，提示「该歌曲暂无可播放音源（VIP / 无版权）」
- 歌词自动获取并滚动
- 电脑版还支持登录后访问更多内容（Cookie 存本机）

### 4.3 哔哩哔哩

B站上一首歌通常是一个视频：3 分钟的 MV、现场版，或「2 小时学习 BGM」合集。按标题和时长挑选。

播放链路：

```
搜索视频(search/type) → 视频信息(view，取 cid)
  → 播放地址(playurl，DASH) → 最高码率音轨 → Media3 原生播放器出声
```

**-412 风控规则（2026-09 实测，各接口相反）：**

| 接口 | 正确姿势 |
|---|---|
| `search/type` 搜索 | **必须带** Referer；部分网络还要新鲜 `buvid3` Cookie，否则 412 风控页 |
| `view` 视频信息 | **不能带** Referer / buvid，带了反而 412，普通 UA 裸奔即可 |
| `playurl` 播放地址 | 同 view，裸奔 |
| `*.bilivideo.com` / `.bilivideo.cn:8082` CDN | **必须带** Referer，否则 403 |

因此代码里搜索与解析分两套请求头，不可统一。

**节点故障自动切换**：playurl 返回主节点 + 备份节点，App 用 Range 请求逐个探测，自动选第一个可达节点；全部失败时稍后重试或换一条结果。

**封面**：视频封面被 Referer 校验时自动走内置图片代理。

### 4.4 电脑版在线能力差异

- 额外支持 **QQ 音乐**（搜索/播放/歌词/封面）
- **B站 MV 模式**：音视频合并、断点续播、全屏
- 登录：扫码 / 网页 / 短信
- 代码位置：[main.js](../main.js) 中的 `NeteaseAPI`、`QQAPI`、`BilibiliAPI`

---

## 五、局域网互通

同一 WiFi 下电脑与手机互相听对方的歌。

- HTTP 传输，默认端口 **15748**（被占用自动 +1～+10）
- UDP 设备发现，端口 **15749**
- 安全：6 位配对码 → 颁发 24 字节随机 token，之后免输入；可随时解除

### 5.1 方向 A：手机听电脑

**电脑侧**：设置 → 局域网 → 打开共享，卡片显示 6 位配对码（可点换码）。

**手机侧**：局域网 Tab 自动发现电脑 → 点电脑名 → 输入 6 位码 → 浏览曲库点歌即播。封面、歌词取自电脑；可下载到手机。token 自动记住，失效（401）时重新弹配对框；兼容 v1 免鉴权主机。

### 5.2 方向 B：电脑听手机

**手机侧**：局域网 Tab →「本机共享」卡片 → 打开开关显示配对码 →「立即同步曲库」。开关状态记住并自启。

**电脑侧**：设置 → 局域网 →「附近的手机」（3 秒自动刷新）→ 点手机名输码 → 浏览播放，音频实时来自手机。

### 5.3 安全规则

| 规则 | 说明 |
|---|---|
| 配对码 | 6 位数字，显示在**主机**（开共享方）屏幕 |
| 错误锁定 | 连续错 **5 次**锁 **60 秒**（按来源 IP） |
| token | 配对成功颁发，24 字节 hex；请求带 `?t=` 或 `X-MSP-Token` 头 |
| 解除配对 | 主机删除设备后 token 立即失效 |
| 未配对 | 仅 `/api/info` 可访问，其余 401 |

### 5.4 发现不到设备？

1. 同一局域网：访客 WiFi 通常开启隔离；手机热点与家用 WiFi 不在一个网段
2. Windows 防火墙：允许 Node.js / MSPMusic 的专用网络访问
3. IP 变化无需手动处理（UDP 自动发现）；端口占用自动顺延
4. **模拟器限制**：模拟器是 NAT 网络，「模拟器手机连宿主机电脑」方向不可靠（各模拟器宿主机别名不同且可能不通），此方向请用真机验证

### 5.5 数据存放

| 端 | 内容 | 位置 |
|---|---|---|
| 电脑 | 共享配置（含配对码） | userData/lan-config.json |
| 电脑 | 已配对设备 | userData/lan-paired.json |
| 电脑（连手机） | 手机 token | localStorage `msp_lan_peer_tokens` |
| 手机 | 配对码/已配对电脑 | SharedPreferences `msp_lan_host` |
| 手机 | 电脑 token | localStorage `msp.lan.tokens` |

协议细节见开发篇第 11 节。

---

## 六、常见卡点 FAQ

### 6.1 电脑版

**Q：没声音？**
1. Windows 音量合成器里 MSPMusic 的音量/静音
2. 设置中输出设备是否正确（USB 声卡/蓝牙耳机切换后需重选）
3. Bit-perfect (mpv) 独占模式会独占声卡，关闭其他播放软件
4. 在线歌：VIP/无版权无地址，换一首
5. 设置 → 运行日志看报错

**Q：点歌进度不走？** 文件被移动/删除 → 刷新音乐库；网络盘未就绪 → 等盘连上。

**Q：封面/信息不对？** 信息来自内嵌标签，改标签后刷新音乐库；无封面时在线匹配需联网。

**Q：歌词不显示/不同步？** 检查同名 `.lrc`；在线抓取需联网；整体偏移用歌词偏移校正。

**Q：打包提示 app.asar 被占用？**

```powershell
taskkill /F /IM MSPMusic.exe; taskkill /F /IM electron.exe
```

删除输出目录后重打，详见 [打包命令.txt](../打包命令.txt)。

### 6.2 安卓版

**Q：白屏很久？** 正常 7～12 秒。超 20 秒：清后台重开；系统刚升级/IO 繁忙会拖慢所有 App。

**Q：添加文件夹扫不出歌？** 确认点了「使用此文件夹/允许」；目录确有支持格式的文件。

**Q：本地歌没声音？** 确认是媒体音量；蓝牙设备重连；`adb logcat | findstr ExoPlayer` 看报错。

**Q：B站搜索失败/转圈？** 确认能打开 bilibili.com；模拟器/机房 IP 可能被限流，换网络。

**Q：个别 B站视频播放失败？** 主/备节点可能全不可用，稍后重试或换结果。

**Q：顶部小黑条 / 内容顶到状态栏？** 已修复；自己改过代码后复现 → 检查 MainActivity edge-to-edge 与 CSS `env(safe-area-inset-*)`。

**Q：横屏封面拉扁？** 已锁正方形；自查 `.np-cover` 相关 CSS 有无被覆盖。

### 6.3 局域网

最常见三条：① 同一 WiFi（访客网络隔离）；② 错码 5 次锁 60 秒；③ 模拟器连宿主机方向用真机测。

### 6.4 抓日志

- 电脑版：设置 → 运行日志
- 安卓版：`adb logcat | findstr MSPMusic`（标签 `MSPMusic/LAN`、`MSPMusic/JS`、`MSPMusic/Player`）

---

# 第二篇：开发篇

> 读者：知道 HTML/CSS/JS 基础即可，不要求提前懂 Electron 或安卓原生。

## 七、基本框架

核心原则三句话：

1. **改界面、交互、文字 → 先找 HTML**（电脑 index.html，手机 mobile.html）
2. **改系统能力（读文件、发请求、出声）→ 找进程侧**（电脑 main.js，手机 Java 桥）
3. 两端视觉同源但是**两份独立代码**，互不自动影响。

### 7.1 电脑版（Electron 双进程模型）

Electron 有两类进程：

- **主进程**（Node.js 环境）：一个，管窗口、文件系统、子进程、系统 API —— [main.js](../main.js)
- **渲染进程**（浏览器环境）：每个窗口一个，跑界面 —— [index.html](../index.html)
- 两者不能直接互访，必须经 IPC；唯一允许暴露能力的地方是 [preload.js](../preload.js)（`contextBridge` 白名单）

```
index.html (渲染进程)
   │  window.electronAPI.xxx()
   ▼
preload.js (桥，白名单 ipcRenderer.invoke)
   │  IPC
   ▼
main.js (主进程 ipcMain.handle)
   ├── 文件扫描 / music-metadata 标签
   ├── NeteaseAPI / QQAPI / BilibiliAPI
   ├── mpv-engine.js（外部 mpv.exe）
   └── lan-server.js（HTTP + UDP）
```

### 7.2 安卓版（Java 外壳 + WebView）

- [MainActivity.java](../apk/project/app/src/main/java/com/mspmusic/app/MainActivity.java)：唯一 Activity，整个界面就是一个全屏 WebView
- 页面源：`https://appassets.androidplatform.net/assets/www/mobile.html`（WebViewAssetLoader 把 APK assets 映射成 https 源）
- JS 经两个注入对象调 Java（`addJavascriptInterface`）：
  - **MSBridge** → [MSBridge.java](../apk/project/app/src/main/java/com/mspmusic/app/MSBridge.java)：本地能力（扫描、标签、文件管理、播放器指令、DSP）
  - **LanBridge** → [LanBridge.java](../apk/project/app/src/main/java/com/mspmusic/app/LanBridge.java)：网络能力（异步 HTTP、配对、下载、本机共享服务控制）
- 真正出声：[PlaybackService.java](../apk/project/app/src/main/java/com/mspmusic/app/PlaybackService.java)（Media3 MediaSessionService + ExoPlayer）
- JS 播放指令链：MSBridge → [PlayerBridge.java](../apk/project/app/src/main/java/com/mspmusic/app/PlayerBridge.java) → ExoPlayer

```
mobile.html (WebView)
   ├── MSBridge.xxx   → 扫描/标签/文件/ExoPlayer 指令/DSP
   ├── LanBridge.xxx  → httpAsync 异步请求 / pair / download / host 控制
   └── <audio> 不承担主播放；主播放全部走原生 ExoPlayer
         ▲
PlaybackService (Media3 ExoPlayer + MediaSession)
   ├── 本地 content://（SAF）
   ├── http(s) 在线直链（HostHeaderDataSourceFactory 自动补 Referer）
   └── dsp/ Java 音效处理
LanHostServer (手机作主机：HTTP + UDP)
```

### 7.3 为什么安卓 UI 也用 HTML

一套前端技能同时维护两端视觉；原生侧只做 HTML 做不了的事：音频解码输出、SAF 文件、后台服务、网络请求绕过跨域。

---

## 八、文件职责地图

### 8.1 项目根

| 文件 | 职责 | 何时动 |
|---|---|---|
| [index.html](../index.html) | 电脑版全部界面（CSS+HTML+JS 单文件） | 电脑端任何视觉/交互 |
| [main.js](../main.js) | 主进程：窗口、扫描、标签、在线 API、托盘、IPC | 文件读取、在线接口、系统行为 |
| [preload.js](../preload.js) | 暴露 `window.electronAPI`（IPC 白名单） | 页面需要新主进程能力 |
| [lan-server.js](../lan-server.js) | 电脑作主机：HTTP 15748 + UDP 15749 | 协议/配对/鉴权 |
| [mpv-engine.js](../mpv-engine.js) | mpv 进程封装 | Bit-perfect 引擎 |
| [login-preload.js](../login-preload.js) | 登录弹窗 preload | 登录窗口 |
| [package.json](../package.json) | 依赖、版本、打包配置 | 版本号、加依赖 |
| [打包命令.txt](../打包命令.txt) | 双端命令速查 | 忘记命令 |

侧边栏导航项（index.html `#sidebar-nav`）：主页、播放列表、专辑、我的收藏、音频输出、调音台、刷新音乐库；footer 为设置。

### 8.2 安卓工程 apk/project/

| 文件 | 职责 |
|---|---|
| [app/build.gradle](../apk/project/app/build.gradle) | versionCode/versionName、SDK 34、Media3 1.4.1 依赖 |
| [AndroidManifest.xml](../apk/project/app/src/main/AndroidManifest.xml) | 权限、Activity、PlaybackService |
| MainActivity.java | WebView 配置、edge-to-edge、SAF 授权、资源路由（/assets /bg /media /cover /stream） |
| PlaybackService.java | ExoPlayer + MediaSession 前台服务 |
| PlayerBridge.java | ExoPlayer 队列/播放/跳转的薄封装 |
| MSBridge.java | 注入对象 `MSBridge`：本地与播放器全部 JS 接口 |
| LanBridge.java | 注入对象 `LanBridge`：HTTP、配对、下载、host 控制 |
| LanHostServer.java | 手机作主机：手写 HTTP/1.1 + UDP 广播、Range/206 |
| HostHeaderDataSourceFactory.java | ExoPlayer 数据源：bilibili 域名自动加 Referer |
| [assets/www/mobile.html](../apk/project/app/src/main/assets/www/mobile.html) | **手机端唯一页面，直接改** |
| assets/www/opencc.js、android-shim.js | 简繁转换、兼容垫片 |
| dsp/ | 音效引擎 + JUnit 测试 |

### 8.3 工作约定（红线）

- mobile.html **直接改** assets 里的那份；**不要**跑 sync-web.ps1
- 改完 mobile.html **必须重新 assembleDebug**
- 不主动 commit；交付 APK 放 `apk/`

---

## 九、数据流详解

### 9.1 安卓本地歌链路

```
点歌曲行 (mobile.html)
 → Native.playQueue(队列数组, 下标)        // mobile.html Native 封装
 → B.playQueue(json, i)                    // B = window.MSBridge
 → MSBridge.playQueue → PlayerBridge.playQueue
 → ExoPlayer 队列（MediaItem；content:// 直接读 SAF）
 → PlaybackService / MediaSession          // 通知栏、蓝牙、息屏
 → 播放器状态回推 JS（mobile.html「状态回推」区）
 → 更新 ps（位置/时长/状态）、mini、进度、歌词
```

关键 JS 状态对象（mobile.html）：

- `store`：持久化设置（folders/songs/favs/theme/close/vol/speed），底层 lsGet/lsSet
- `songs`：本地曲库（来自 `store.songs`）
- `queue`：当前队列镜像
- `ps`：播放状态 `{id,index,playing,posMs,durMs,repeat,shuffle,speed}`

### 9.2 安卓在线歌链路

```
Online.searchBili/searchNetease
 → LanBridge.httpAsync(url, headers, 0)    // Java 后台单线程
 → JS 轮询 takeHttp(id)：{s:0 进行中 | s:1,t:正文 | s:2,code:错误码}
播放：
 网易云 enhance/player/url?ids=[..]&br=320000 → 直链
 B站 view(cid) → playurl(dash) → 节点探测 → 直链
 → 作为 http MediaItem 进 ExoPlayer
 → HostHeaderDataSourceFactory.open() 按 host 分流补 Referer
```

httpAsync 是在线网络**唯一入口**：避免 JS 跨域、统一 UA、非 2xx 会在 logcat 打印状态码。

### 9.3 电脑版链路

```
index.html
 → electronAPI.xxx() → preload.js → ipcMain.handle (main.js)
本地播放：
 Bit-perfect：mpv-engine.js → mpv.exe（独占可选）
 常规通路：HTML audio / Web Audio 调音图
在线：NeteaseAPI / QQAPI / BilibiliAPI（主进程，带限速）
共享：lan-server.js
```

main.js 关键设施：`SUPPORTED_EXTS`、`walkMusicDir/scanAudioFiles`（扫描，CUE 解析）、`MusicMetaFetcher`（music-metadata 标签）、元数据缓存（META_CACHE_VERSION=2）、metaQueue 刮取队列、日志环 logRing、托盘。

---

## 十、「想改什么效果 → 动哪里」对照表

> 定位法：用 VS Code 搜索（Ctrl+F）下表里的**横幅注释/类名/文案**，比行号耐用。
> index.html 的 CSS 区横幅是 `/* ── 名字 ── */`，JS 区是 `// ── 名字 ──`；mobile.html 是 `/* ═══ 名字 ═══ */`。

### 10.1 视觉类

| 我想… | 改哪里 | 搜什么 |
|---|---|---|
| 改主色/强调色 | index.html、mobile.html 的 `:root` 变量 | `--accent`、`--primary` |
| 改圆角 | `:root` 的 `--radius` 系列 | `--radius` |
| 改字体/字号 | `:root` 字体变量 + font-family | `font-family`、`--font` |
| 改深色/浅色色板 | `[data-theme="dark"]` / `[data-theme="light"]` | `data-theme` |
| 改动画速度/手感 | 统一缓动与时长变量 | `cubic-bezier`、`--ease` |
| 改封面尺寸/圆角 | 全屏页/mini CSS | `.np-cover`、`.mini-cover` |
| 改毛玻璃强度 | backdrop-filter 处 | `backdrop-filter` |
| 改侧栏宽度 | 侧栏 CSS | `#sidebar`、`.sidebar` |

### 10.2 播放与歌词

| 我想… | 改哪里 |
|---|---|
| 默认播放模式/音量/倍速 | mobile.html 默认值在 store 初始化；搜 `msp.vol` `msp.speed` |
| 歌词滚动跟随延迟 | mobile.html 歌词区，搜 `2.5` / `lyric` |
| 歌词高亮样式 | CSS `.lrc-line.active` 类 |
| 加一个新的倍速档位 | mobile.html 倍速选择 UI + `B.setSpeed` 调用 |
| AB 复读逻辑 | mobile.html AB 相关函数；电脑 index.html 同 |
| 随机策略 | 安卓 PlayerBridge/Media3 shuffle；电脑 index.html 随机函数 |

### 10.3 在线音源

| 我想… | 改哪里 |
|---|---|
| 调 B站请求头 | mobile.html Online 区（search/type 与 view/playurl 两套头）；电脑 main.js `BilibiliAPI` |
| 改音质/码率 | 网易云 `br=320000`；B站 playurl `qn`/dash 选取逻辑 |
| 加一个新音源 | mobile.html：chip UI + search 分发 + 解析播放 URL 逻辑 + （需要的话）HostHeader 域名白名单；电脑 main.js 仿 `QQAPI` 加对象 + IPC |
| 改搜索结果分页/条数 | `search/type?page=`、网易云 `limit/offset` 处 |

### 10.4 局域网

| 我想… | 改哪里 |
|---|---|
| 改默认端口/端口范围 | lan-server.js、LanHostServer.java、LanBridge.java 中的 15748/+1～+10、UDP 15749（改要两端同步） |
| 改配对码长度/锁定规则 | lan-server.js 与 LanHostServer.java 的 PIN 生成与失败计数 |
| 改协议版本 | `PROTOCOL_VERSION`（两端都改） |
| 增加一个 API 端点 | 主机侧路由：lan-server.js `handleRequest` 或 LanHostServer.java `dispatch`；客户端调用方 |
| 改对外暴露字段 | lan-server.js `/api/tracks` 的 safe 映射 |

### 10.5 版本与权限

| 我想… | 改哪里 |
|---|---|
| 发新版（电脑） | package.json `version` |
| 发新版（安卓） | app/build.gradle `versionCode`（必须+1）+ `versionName`，APK 文件名同步 |
| 改应用名/图标 | strings.xml `app_name`；mipmap 图标资源 |
| 加新安卓权限 | AndroidManifest.xml（能不加就不加） |

#### 10.5.1 发版必改的 4 处版本号（重要）

「关于」页里显示的版本号是**写死在页面里的**，不会自动跟随 package.json / build.gradle。所以发一次版，一共要改 **4 处**，漏改就会出现「安装包是新版、关于页还是旧版」：

| # | 文件 | 改什么 | 影响范围 |
|---|---|---|---|
| 1 | [package.json](../package.json) | `"version": "26.9.13"` | 安装包文件名（MSPMusic-Setup-26.9.13.exe）、系统识别版本 |
| 2 | [index.html](../index.html#L4196)（设置 → 关于） | 搜 `v26.9.13.1`，改显示文本 | **电脑「关于」页显示的版本** |
| 3 | [apk/project/app/build.gradle](../apk/project/app/build.gradle) | `versionCode` 每次 **+1**（整数，用于升级判断）+ `versionName '2.4.0'` | 安卓系统设置中的版本、能否覆盖升级 |
| 4 | [mobile.html](../apk/project/app/src/main/assets/www/mobile.html#L755)（设置 → 关于） | 搜 `v2.4.0`，改显示文本 | **安卓「关于」页显示的版本**，改完需重新构建 APK |

注意事项：

- 第 2 处目前是四段式 `v26.9.13.1`，与 package.json 的三段式不一致——这是历史遗留，改的时候按你自己的命名规则统一即可，两处不强制相同。
- 安卓 JS 侧**没有**读取 `versionName` 的接口（MSBridge 未暴露），所以第 4 处无法自动同步，只能手改。
- 交付的 APK 文件名（如 `apk\MSPMusic-a-m10-*.apk`）里的版本标识也是手动命名，别忘了和 build.gradle 对齐。

### 10.6 改代码最小流程（手机端举例）

1. 改 [mobile.html](../apk/project/app/src/main/assets/www/mobile.html)
2. 一行语法检查（见 14.4）
3. 构建：gradle `assembleDebug`
4. `adb install -r` 装机验证
5. 交付 APK 复制到 `apk/`

---

## 十一、局域网协议详解

协议版本 `PROTOCOL_VERSION = 2`，HTTP/1.1 + JSON。v1（无鉴权）仍被客户端兼容。

### 11.1 端点清单

**电脑主机（lan-server.js）与手机主机（LanHostServer.java）语义一致：**

| 方法 路径 | 鉴权 | 作用 |
|---|---|---|
| GET `/api/info` | 无 | 返回 `{app,v,kind,name,count,auth,ips}` |
| POST `/api/pair` | 无 | body 含配对码；成功返回 token；错误计入锁定 |
| GET `/api/tracks` | token | 摘要数组：`{id,title,artist,album,duration,ext}`（不下发 path） |
| GET `/api/stream/:id` | token | 音频流，支持 Range → 206 |
| GET `/api/download/:id` | token | 同流但带 `Content-Disposition` 文件名 |
| GET `/api/cover/:id` | token | 封面 |
| GET `/api/lrc/:id` | token | 歌词 |
| OPTIONS * | 无 | CORS 预检 |

鉴权方式：query `?t=<token>` 或头 `X-MSP-Token`。失败 401 JSON。

### 11.2 UDP 发现（15749）

- 主机周期性广播自己的存在（含设备名、HTTP 端口、协议版本）
- 客户端也可主动探测，主机应答
- 客户端据此渲染设备列表；IP/端口变化自动跟随，不需要用户手填

### 11.3 配对与锁定流程

```
POST /api/pair {pin:"123456"}
  ├─ 正确 → 生成 24 字节 hex token，落盘（lan-paired.json / SharedPreferences）
  │         响应 {ok:true, token}
  └─ 错误 → 按 IP 计数；第 5 次起 60 秒内一律拒绝
```

已配对设备再次请求用 token；主机删除设备 → token 立刻失效。

### 11.4 手机主机实现要点（LanHostServer）

- 手写 HTTP/1.1：多线程 accept，支持 keep-alive 与 Range
- `/api/stream` 从 SAF Uri 读取并按 Range 输出 206
- tracks 由 JS 侧「立即同步曲库」推给服务端
- 已知遗留风险：中文设备名建议按原始字节 UTF-8 解码 body，防乱码

### 11.5 扩展协议的注意事项

1. 破坏性变更必须升 `PROTOCOL_VERSION`，两端同步
2. 新端点默认放 token 鉴权之后
3. 不要在 tracks/info 中下发绝对路径等本机信息
4. CORS 头保持白名单，不要随意放开自定义头

---

## 十二、DSP 音效引擎详解

DSP 在 Java 侧处理 PCM，位置 `com.mspmusic.app.dsp`，由 JUnit 测试守护（`gradle testDebugUnitTest`）。

### 12.1 处理结构

- `MspDspProcessor`：聚合各模块的总处理器，按启用顺序串接
- `DspModule`：统一模块接口（处理浮点 PCM 缓冲、参数读写、开关）
- 模块参数可由 mobile.html 经 MSBridge 设置；调音台 UI 与参数一一对应

### 12.2 模块一览（调音台）

| 模块 | 作用 |
|---|---|
| 均衡器 EQ | 多段频率增益 |
| 混响 Reverb | 空间感、衰减时间、干湿比 |
| 延迟校正 Delay | 对齐/简单回声 |
| 动态控制 Dynamics | 压缩/限幅，防削峰 |
| Crossfeed | 耳机串音模拟，接近音箱听感 |
| 声场/声道矩阵 | 左右声像、声道映射 |

预设与脉冲响应（IR）资源随 APK 打包；切换歌曲/设备时处理器状态平滑过渡避免爆音。

### 12.3 改 DSP 的建议路径

1. 先跑 `testDebugUnitTest`，确认基线绿
2. 改模块算法 → 同步改/加 JUnit（已知输入→已知输出）
3. 加参数：DspModule 参数 + MSBridge 暴露 + mobile.html 调音台控件
4. 验证：纯音/扫频信号过处理，观察削波、NaN、边界（0 长度缓冲、立体声/单声道）

---

## 十三、开发环境、构建与打包

### 13.1 路径（本机，全部免安装版）

| 工具 | 路径 |
|---|---|
| JDK 17 | `e:\aixuniji\android-build-tools\jdk17` |
| Android SDK | `e:\aixuniji\android-build-tools\sdk` |
| Gradle 8.7 | `e:\aixuniji\android-build-tools\gradle-8.7\bin\gradle.bat` |
| adb | `E:\aixuniji\android-build-tools\sdk\platform-tools\adb.exe` |
| 模拟器 | 雷电模拟器 9，adb 设备 `127.0.0.1:5555`，竖屏 1080×1920 |

### 13.2 电脑版

```powershell
npm install          # 装依赖
npx electron .       # 开发运行
# 打包详见 打包命令.txt（先杀进程再删输出目录，防 app.asar 占用）
```

### 13.3 安卓构建（单元测试 + APK 一条龙）

```powershell
$env:JAVA_HOME='e:\aixuniji\android-build-tools\jdk17'
$env:ANDROID_HOME='e:\aixuniji\android-build-tools\sdk'
$env:PATH="$env:JAVA_HOME\bin;$env:PATH"
& 'e:\aixuniji\android-build-tools\gradle-8.7\bin\gradle.bat' `
  -p e:\aixuniji\music\apk\project testDebugUnitTest assembleDebug --no-daemon
```

产物：`apk\project\app\build\outputs\apk\debug\app-debug.apk`，交付时复制为 `apk\MSPMusic-a-*.apk`。

装机：

```powershell
E:\aixuniji\android-build-tools\sdk\platform-tools\adb.exe connect 127.0.0.1:5555
E:\aixuniji\android-build-tools\sdk\platform-tools\adb.exe install -r <apk路径>
```

---

## 十四、调试方法大全

### 14.1 chrome://inspect 远程调试 WebView（最常用）

1. App 开着，USB/模拟器连接正常
2. 电脑 Chrome 打开 `chrome://inspect/#devices`
3. 勾选 Discover network targets；出现 `WebView in com.mspmusic.app` → inspect
4. 等同 DevTools：断点、Console、Elements、Application(localStorage)

### 14.2 logcat

```powershell
# 全部相关
adb logcat -s MSPMusic/LAN MSPMusic/JS MSPMusic/Player
# ExoPlayer / Media3
adb logcat | findstr /i "ExoPlayer Media3"
# 清屏后只看新日志
adb logcat -c
```

### 14.3 电脑端

- 渲染进程：窗口按 F12 / Ctrl+Shift+I（已开 DevTools）
- 主进程：`npx electron .` 的终端输出；设置 → 运行日志（logRing）

### 14.4 mobile.html JS 语法检查（一行，不建文件）

```powershell
node -e "const fs=require('fs');const h=fs.readFileSync('e:/aixuniji/music/apk/project/app/src/main/assets/www/mobile.html','utf8');const m=[...h.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/gi)];let n=0;for(const s of m){n++;new Function(s[1]);}console.log('OK scripts='+n);"
```

只校验语法不执行，安全。改完先跑这个再构建。

### 14.5 模拟器截图坑

模拟器截图工具截不到 WebView 内容时，用 `adb exec-out screencap -p > x.png` 或 chrome://inspect 里截图。

---

## 十五、持久化数据清单

改动涉及「用户数据」时先查此表，防止清数据或迁版本翻车。

### 15.1 安卓 localStorage（WebView 源 https://appassets.androidplatform.net）

| 键 | 内容 |
|---|---|
| `msp.folders.v2` | SAF 文件夹授权列表 |
| `msp.songs.v2` | 本地曲库缓存 |
| `msp.favs` | 收藏 |
| `msp.theme` | 主题 |
| `msp.close` | 关闭面板等 UI 状态 |
| `msp.vol` / `msp.speed` | 音量 / 倍速 |
| `msp.search.src` | 搜索音源 |
| `msp.lan.tokens` | 电脑主机 token |
| `msp.lan.host` | 本机共享开关等 |

### 15.2 安卓 SharedPreferences

`msp_lan_host`：配对码、已配对电脑、host 服务状态。

### 15.3 电脑 userData（`%APPDATA%\MSPMusic`）

| 文件 | 内容 |
|---|---|
| lan-config.json | 共享配置（含配对码） |
| lan-paired.json | 已配对设备/token |

localStorage 键：`msp_lan_peer_tokens`（电脑连手机的 token）。

### 15.4 版本迁移规则

- 缓存键带 `.v2` 后缀：结构破坏性变更时升版本号并写迁移/丢弃逻辑，不要静默改旧结构
- 电脑端元数据缓存 META_CACHE_VERSION=2，同理

---

## 十六、设计与动画规范

两端共用一套视觉语言，改样式请遵守，保证手感统一。

- **主色**：低饱和靛蓝（强调色），语义色：网易云红 `#e03a3e`、B站粉 `#fb7299`
- **缓动**：统一 `cubic-bezier(.2, 0, .2, 1)`
- **时长**：小元素（按钮/chip）120–160ms；大面板/全屏切换 180–220ms
- **只动 `transform` / `opacity`**（不触发重排）；不做弹簧动画
- 圆角、间距、毛玻璃强度走 CSS 变量，不要散落硬编码
- 深色/浅色两套色板都要验证；自定义背景下所有层级都有底色兜底
- 安全区：所有贴边布局使用 `env(safe-area-inset-*)`

---

## 十七、历史修复（别回退）与踩坑清单

### 17.1 别回退的修复（10 条）

1. 安卓 edge-to-edge：内容不再顶状态栏/小黑条（MainActivity + safe-area CSS）
2. 横屏封面锁正方形（`.np-cover` 纵横比 1/1）
3. B站 search/type 带 Referer+buvid、view/playurl 裸奔——方向相反，勿统一
4. bilivideo CDN 请求必须 Referer（HostHeaderDataSourceFactory）
5. B站节点 Range 探测选活节点
6. 封面 Referer 裂图自动走图片代理
7. 刮取增量刷新、不重建列表
8. 网易云整页批量解析播放地址，避免逐首等待
9. 错码 5 次锁 60 秒、24 字节 token 鉴权
10. 安卓 SAF 授权 + 不申请存储权限

### 17.2 踩坑清单（9 条）

1. 不要跑 sync-web.ps1，直接改 assets 下 mobile.html
2. 改完 mobile.html 必须重新构建 APK，刷新安装才生效
3. 长文档/长写入分段提交，防截断
4. 模拟器 NAT 下「手机连宿主机」方向不可靠，用真机
5. WebView 页面源是 https appassets 映射，localStorage 按该源隔离
6. 在线请求一律走 LanBridge.httpAsync，不要在 JS 里 fetch（跨域+UA）
7. 打包前杀进程、删输出目录，防 app.asar 占用
8. 破坏性数据结构变更升 `.v2` / CACHE_VERSION
9. DSP 改动必须配 JUnit，防算法静默回归

---

## 十八、验证清单与上手路线

### 18.1 改动后验证清单

**视觉改动**：深色/浅色各看一遍；横屏竖屏；自定义背景开/关；安全区（有刘海机型）。

**播放改动**：本地歌、网易云、B站各一首；上一首/下一首；随机+循环；倍速；息屏+通知栏；蓝牙耳机按键；AB 复读。

**局域网改动**：两个方向各配一次对；错码锁定；删配对后 401；发现列表；断网恢复。

**DSP 改动**：先 `testDebugUnitTest`；无削波爆音；开关切换平滑。

### 18.2 最小上手路径（第一次改代码）

1. 用 VS Code 打开项目根目录
2. 打开 [mobile.html](../apk/project/app/src/main/assets/www/mobile.html)，Ctrl+F 搜 `═══` 浏览全部代码分区
3. 改一个文案（比如设置页某行字）
4. 跑 14.4 的语法检查
5. gradle 构建 + adb 安装，看效果
6. 再尝试改一个 `:root` 颜色变量
7. 最后再碰数据流/Java 侧

---

# 附录

## 附录 A：支持的音频格式

| 类型 | 扩展名 |
|---|---|
| 有损 | `.mp3` `.aac` `.m4a` `.ogg` `.opus` `.wma` `.mka` |
| 无损 | `.flac` `.wav` `.aiff` `.alac`(`.m4a`) |
| 其他 | `.cue`（整轨分轨） |

最终以 main.js `SUPPORTED_EXTS` 与 ExoPlayer 解码能力为准；DTS/部分冷门编码不保证。

## 附录 B：快捷键一览

| 按键 | 作用 |
|---|---|
| Space | 播放 / 暂停 |
| ← / → | 快退 / 快进 |
| ↑ / ↓ | 音量 |
| Ctrl+F | 聚焦搜索 |
| Ctrl+Shift+I / F12 | DevTools |

（以 index.html 快捷键绑定代码为准；移动端无快捷键。）

## 附录 C：术语表

| 术语 | 含义 |
|---|---|
| 主进程 / 渲染进程 | Electron 的 Node 进程 / 浏览器窗口进程 |
| preload | 渲染进程加载前注入的桥脚本，安全白名单 |
| IPC | 进程间通信 |
| SAF | Android Storage Access Framework，系统文件选择器授权 |
| Media3 / ExoPlayer | AndroidX 媒体库与播放器实现 |
| MediaSession | 系统媒体会话（通知栏/锁屏/耳机按键） |
| DSP | 数字信号处理（均衡/混响等） |
| DASH | B站音视频分离的流式格式 |
| token | 配对后颁发的访问凭证 |
| userData | Electron 应用数据目录（`%APPDATA%\MSPMusic`） |

---

## 免责声明

- 本软件仅供学习与个人本地音乐管理使用
- 在线音源音频流来自平台官方 CDN，版权归原平台及权利人所有；VIP / 无版权内容无法播放，请支持正版
- 局域网共享请仅在可信网络中开启，并妥善保管配对码；因共享造成的内容泄露风险由使用者自行承担
- 账号登录在平台官方页面完成，本软件不获取、不上传账号密码

---
## 开源声明
- 本项目在动画打磨好之后就会开源
  
  目前文档内容即开源后的内容

*— 本手册基于电脑版 26.9.16 / 安卓版 2.4.0 实际代码编写，如代码更新请同步修订。*

